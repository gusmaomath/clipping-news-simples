fastapi>=0.110
uvicorn[standard]>=0.27
feedparser>=6.0
trafilatura>=1.8
APScheduler>=3.10
httpx>=0.27
pydantic>=2.0
# Opcional — só para o provedor Claude (Anthropic). OpenAI e Gemini funcionam via httpx (REST), sem SDK.
anthropic>=0.40
